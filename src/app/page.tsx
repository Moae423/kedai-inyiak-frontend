import React from "react";
import FormLogin from "@/components/Auth/FormLogin";

const Home = () => {
  return (
    <div className="flex items-center justify-center gap-5 min-h-screen p-8">
      <FormLogin />
    </div>
  );
};

export default Home;
